interface:
  display_name: "Orchestrate Skills"
  short_description: "Route broad tasks into confirmed, portable Skill pipelines."
  default_prompt: "Analyze my task, scan available local skills, propose a cross-agent compatible pipeline, and wait for confirmation before creating a goal and executing."
